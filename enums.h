#ifndef ENUMS_H
#define ENUMS_H

//TokenTypes
typedef enum{
    EXIT,
}TypeKeyword;
typedef enum{
    INT,
}TypeLiteral;
typedef enum{
    SEMI,
    OPEN_PAREN,
    CLOSED_PAREN,
}TypeSeparator;

#endif